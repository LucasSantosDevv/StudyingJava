package classe;

/*                              "Uma classe define um tipo"

    Isso significa que uma classe serve como molde para criação de objetos distintos, mas como se criar uma classe?
basicamente pode-se usar tipos do java (como 'Strings', primitivos e outros) para a montagem de um tipo único.   */

public class Objeto {

   public String nome;
    double preco;
    double desconto;

    public Objeto(String nome) {
        this.nome = nome;
    }

    // Aqui usamos os tipos Java para criação da classe única 'Produto'.
}
